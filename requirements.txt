DB.Arithmetic.push(
    () => {
        const p = 1000 * (Math.floor(Math.random()*9)+1);
        const r = 10, t = 2;
        const si = (p * r * t) / 100;
        const ci = p * (Math.pow(1 + r/100, t) - 1);
        return { tag: "SI & CI | Difference", text: `Find the difference between the Compound Interest and Simple Interest on ₹${p} for ${t} years at ${r}% per annum.`, ans: `₹${(ci - si).toFixed(2)}`, steps: `1. SI = (P*R*T)/100 = ${si}. 2. CI = P(1+R/100)^T - P = ${ci.toFixed(2)}. 3. Difference = ${ci.toFixed(2)} - ${si}.` };
    },
    () => {
        const s1 = 30 + Math.floor(Math.random()*20);
        const s2 = s1 + 10;
        const avg = (2 * s1 * s2) / (s1 + s2);
        return { tag: "TSD | Average Speed", text: `A car travels from A to B at ${s1} kmph and returns at ${s2} kmph. Find the average speed for the entire journey.`, ans: `${avg.toFixed(2)} kmph`, steps: `1. Formula: 2xy/(x+y). 2. 2(${s1}*${s2}) / (${s1}+${s2}) = ${avg.toFixed(2)}.` };
    },
    () => {
        const cp = 400 + (Math.floor(Math.random()*10)*50);
        const mp = cp * 1.5;
        const disc = 20;
        const sp = mp * (1 - disc/100);
        const profit = ((sp - cp)/cp)*100;
        return { tag: "P&L | Discount", text: `A shopkeeper marks his goods ${50}% above the cost price (₹${cp}) and allows a discount of ${disc}%. Find his profit percentage.`, ans: `${profit}%`, steps: `1. MP = ${cp} * 1.5 = ${mp}. 2. SP = ${mp} * 0.8 = ${sp}. 3. Profit % = ((${sp}-${cp})/${cp})*100 = ${profit}%.` };
    },
    () => {
        const a = 12, b = 15, c = 20;
        const rate = (1/a + 1/b + 1/c);
        const time = 1/rate;
        return { tag: "Time & Work | 3 Variables", text: `A, B, and C can complete a work in ${a}, ${b}, and ${c} days respectively. How many days will they take to finish it together?`, ans: `${time.toFixed(1)} days`, steps: `1. Combined Rate = 1/${a} + 1/${b} + 1/${c} = 12/60 = 1/5. 2. Time = 5 days.` };
    },
    () => {
        const x = 10 + Math.floor(Math.random()*10);
        const y = x + 5;
        const ratio = "2:3";
        return { tag: "Ratios | Age", text: `The ages of P and Q are in ratio ${ratio}. ${y} years hence, the ratio becomes 3:4. Find P's current age.`, ans: `${x * 2} years`, steps: `1. Let ages be 2x, 3x. 2. (2x+${y})/(3x+${y}) = 3/4. 3. 8x + ${4*y} = 9x + ${3*y} -> x = ${y}.` };
    }
);
DB.Algebra.push(
    () => {
        const base = 3, p = 4;
        const val = Math.pow(base, p);
        return { tag: "Logs | Properties", text: `Solve for x: log<sub>${base}</sub>(x) + log<sub>${base}</sub>(3) = log<sub>${base}</sub>(${val * 3})`, ans: val, steps: `1. log(3x) = log(${val*3}). 2. 3x = ${val*3} -> x = ${val}.` };
    },
    () => {
        const a = 5, d = 3, n = 20;
        const tn = a + (n-1)*d;
        return { tag: "AP | Terms", text: `Find the ${n}th term of an Arithmetic Progression where the first term is ${a} and common difference is ${d}.`, ans: tn, steps: `1. T<sub>n</sub> = a + (n-1)d. 2. ${a} + (19)*${d} = ${tn}.` };
    },
    () => {
        const b = 7, c = 12;
        return { tag: "Quadratics | Roots", text: `Find the sum of the roots and the product of the roots for x² - ${b}x + ${c} = 0.`, ans: `Sum: ${b}, Product: ${c}`, steps: `1. Sum = -b/a = ${b}. 2. Product = c/a = ${c}.` };
    },
    () => {
        const x = 2;
        return { tag: "Functions | Inverse", text: `If f(x) = (3x + 4)/2, find f⁻¹(5).`, ans: x, steps: `1. Let (3x+4)/2 = 5. 2. 3x+4 = 10 -> 3x = 6 -> x = 2.` };
    },
    () => {
        const n = 10;
        const sum = (n * (n + 1) * (2 * n + 1)) / 6;
        return { tag: "Series | Squares", text: `Find the sum of the squares of the first ${n} natural numbers.`, ans: sum, steps: `1. Formula: [n(n+1)(2n+1)]/6. 2. [10*11*21]/6 = ${sum}.` };
    }
);